diff --git a/src/helpers/ota/OtaManager.cpp b/src/helpers/ota/OtaManager.cpp
--- a/src/helpers/ota/OtaManager.cpp
+++ b/src/helpers/ota/OtaManager.cpp
@@ -913,6 +913,35 @@ void OtaManager::loop() {
 
 // ---------------- dispatch ----------------
 
+bool OtaManager::terminallyConsumes(const uint8_t* msg, uint16_t len) {
+  switch (ota_msg_type(msg, len)) {
+    case OTA_REQ: {
+      ReqMsg rq;
+      if (!decode_req(msg, len, rq)) return false;
+      ServeView* v = resolve(rq.manifest_id);
+      return v && rq.block_idx < v->m.block_count;
+    }
+    case OTA_REQ_PROOF: {
+      ReqProofMsg rp;
+      if (!decode_req_proof(msg, len, rp)) return false;
+      ServeView* v = resolve(rp.manifest_id);
+      return v && rp.block_idx < v->m.block_count;
+    }
+    case OTA_DATA: {
+      DataMsg dm;
+      return decode_data(msg, len, dm) && _fetch && _fstate != IDLE
+          && dm.block_idx < _fbc && memcmp(dm.manifest_id, _fid, 4) == 0;
+    }
+    case OTA_PROOF: {
+      ProofMsg pm;
+      return decode_proof(msg, len, pm) && _fetch && _fstate != IDLE
+          && pm.block_idx < _fbc && memcmp(pm.manifest_id, _fid, 4) == 0;
+    }
+    default:
+      return false;
+  }
+}
+
 void OtaManager::on_message(const uint8_t* msg, uint16_t len) {
   switch (ota_msg_type(msg, len)) {
     case OTA_ADV:          handleAdv(msg, len); break;
diff --git a/src/helpers/ota/OtaManager.h b/src/helpers/ota/OtaManager.h
--- a/src/helpers/ota/OtaManager.h
+++ b/src/helpers/ota/OtaManager.h
@@ -287,6 +287,9 @@ public:
   // This node's id (pubkey[0:4]), stamped into adverts we send so receivers can count distinct seeders.
   void set_seeder_id(const uint8_t* id4) { if (id4) for (int i = 0; i < 4; i++) _seeder_id[i] = id4[i]; }
 
+  // True when this node is the source or destination for a bulk message. The
+  // mesh adapter uses this to avoid re-flooding traffic already consumed here.
+  bool terminallyConsumes(const uint8_t* msg, uint16_t len);
   void on_message(const uint8_t* msg, uint16_t len);   // feed one received OTA message
   void loop();                                         // drive fetch (re-request missing blocks)
 
